# Example configuration for AWS Bedrock Nova provider
server:
  port: 3000
  host: "localhost"

aws:
  region: "ap-southeast-1"

mcp:
  server_name: "aws-infrastructure-server"
  version: "1.0.0"

agent:
  provider: "bedrock"                # Use AWS Bedrock Nova
  model: "apac.amazon.nova-pro-v1:0" # Options: nova-micro-v1:0, nova-lite-v1:0, nova-pro-v1:0
  max_tokens: 10000
  temperature: 0.1
  dry_run: true
  auto_resolve_conflicts: false
  enable_debug: true
  # Note: Configure AWS credentials using: aws configure, environment variables, or IAM roles

logging:
  level: "info"
  format: "text"
  output: "stdout"

state:
  file_path: "./states/infrastructure-state.json"
  backup_enabled: true
  backup_dir: "./backups"

web:
  port: 8080
  host: "localhost"
  template_dir: "web/templates"
  static_dir: "web/static"
  enable_websockets: true
